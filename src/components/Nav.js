import React from "react";
import Navbar from "react-bootstrap/Navbar";
import Container from "react-bootstrap/Container";

// simple Navbar component
const Nav = ({}) => (
  <Navbar className="lina-theme">
    <h3 className="m-2">Lina</h3>
  </Navbar>
);

export default Nav;
